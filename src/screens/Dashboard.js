import React, { useEffect, Fragment } from "react";
import { useSelector } from "react-redux";

const Dashboard = ({ history }) => {
  
  return <h1>All my graphs here</h1>;
};

export default Dashboard;
